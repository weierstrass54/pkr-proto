server:
  error:
    whitelabel:
      enabled: false
spring:
  application:
    name: exam
  datasource:
    url: jdbc:postgresql://localhost:5432/test-exam
    username: test-exam
    password: test-password
  flyway:
    locations: classpath:migrations
  mvc:
    throw-exception-if-no-handler-found: true
