server:
  port: 8090
  error:
    whitelabel:
      enabled: false
spring:
  application:
    name: websocket
  cloud:
    zookeeper:
      connect-string: localhost:2181
  mvc:
    throw-exception-if-no-handler-found: true
ribbon:
  eureka:
    enabled: false