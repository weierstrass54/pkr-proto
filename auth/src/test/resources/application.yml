server:
  error:
    whitelabel:
      enabled: false
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/test-auth
    username: test-postres
    password: test-postgres
  flyway:
    locations: classpath:migrations
  mvc:
    throw-exception-if-no-handler-found: true
